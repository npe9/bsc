module Main (main) where

import BlueTcl
import System.Environment (getArgs)

-- The real main function is in bluetcl_Main.c
-- This is just a stub to satisfy the build system
main :: IO ()
main = return () 